@.pdeq/AGENTS.md

# Shepherd — Project-Specific Overrides

The framework above (imported from `.pdeq/AGENTS.md`) defines the coordinator agent.

Project identity, platforms, tech stack, and operating instructions (including the project-specific slash commands `/shepherd`, `/shepherd-review`, `/land`, `/readme`) live in [`project.md`](project.md). Read it at the start of every session.
